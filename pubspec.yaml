name: alcohol_neon
description: 'AtomiCloud alcohol mobile app'
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.11.4

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # Auth (Logto) — uses flutter_web_auth_2 (ASWebAuthenticationSession on iOS),
  # flutter_secure_storage for token persistence.
  logto_dart_sdk: ^3.0.0
  # HTTP client for the zinc REST API + Logto.
  http: ^1.2.2
  # Lightweight state/DI for the auth service.
  provider: ^6.1.2
  json_annotation: ^4.12.0
  flutter_timezone: ^5.1.0
  url_launcher: ^6.3.2
  # Storefront country for the subscription CTA steering rules: StoreKit
  # storefront (iOS) + Play Billing config (Android). Platform packages only —
  # we don't run purchases through the umbrella in_app_purchase plugin.
  in_app_purchase_storekit: ^0.4.0
  in_app_purchase_android: ^0.4.0
  in_app_purchase_platform_interface: ^1.4.0
  airwallex_payment_flutter: ^0.2.0
  flutter_local_notifications: ^19.0.0
  timezone: ^0.10.0
  # App's own bundle id at runtime → AppConfig selects the landscape (bundle-id-as-marker).
  package_info_plus: ^8.0.0
  # Parse the layered config/*.yaml at startup.
  yaml: ^3.1.2
  # Persist dev-menu config overrides to an app-private JSON file.
  path_provider: ^2.1.5
  # Lottie loading animation (the sloth loader, shared with argon).
  lottie: ^3.1.2

# Pin path_provider_foundation below the version that adopted Flutter's experimental
# native-assets/FFI (objective_c), which doesn't build cleanly under the nixpkgs Flutter
# toolchain. 2.4.x uses the classic pigeon/method-channel implementation.
dependency_overrides:
  path_provider_foundation: 2.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  # Generates iOS/Android launcher icons from the logo (see flutter_launcher_icons: below).
  flutter_launcher_icons: ^0.14.1
  swagger_parser: ^1.43.1
  build_runner: ^2.15.0
  json_serializable: ^6.14.0
  # iOS flavor generation (schemes + per-flavor bundle ids). Run ONLY the iOS
  # processors so it never touches the hand-built Android flavors:
  #   flutter pub run flutter_flavorizr -p ios:xcconfig,ios:buildConfig,ios:plist,ios:schema
  flutter_flavorizr: ^2.2.3

# App-icon generation. Run: dart run flutter_launcher_icons
# iOS icons can't be transparent, so the transparent logo is flattened onto a background.
flutter_launcher_icons:
  android: true
  ios: true
  image_path: 'assets/brand/logo.png'
  remove_alpha_ios: true
  background_color_ios: '#FFFFFF'
  adaptive_icon_background: '#FFFFFF'
  adaptive_icon_foreground: 'assets/brand/logo.png'

# iOS flavors. Android flavors are hand-maintained in android/app/build.gradle.kts,
# so only run flutter_flavorizr's iOS processors (see the dev_dependency note).
# iOS bundle ids match the App Store Connect apps + the Logto callback schemes.
flavorizr:
  flavors:
    pichu:
      app:
        name: 'LazyTax (Pichu)'
      ios:
        bundleId: 'cloud.atomi.alcohol.neon.pichu'
      android:
        applicationId: 'cloud.atomi.alcohol_neon.pichu'
    pikachu:
      app:
        name: 'LazyTax (Pikachu)'
      ios:
        bundleId: 'cloud.atomi.alcohol.neon.pikachu'
      android:
        applicationId: 'cloud.atomi.alcohol_neon.pikachu'
    raichu:
      app:
        name: 'LazyTax'
      ios:
        bundleId: 'cloud.atomi.alcohol.neon.raichu'
      android:
        applicationId: 'cloud.atomi.alcohol_neon.raichu'

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/brand/
    - assets/animations/
    - config/base.yaml
    - config/pichu.yaml
    - config/pikachu.yaml
    - config/raichu.yaml

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
